import { Meta, Props, Story, Preview } from '@storybook/addon-docs/blocks';
import { action } from "@storybook/addon-actions";
import { linkTo } from '@storybook/addon-links'

import FDatePicker from '../packages/figleaf/components/datepicker/FDatePicker';

<Meta title="Components/DatePicker" component={FDatePicker} />

# Date Picker

<Props of={FDatePicker} />

This component is based on <a href="https://flatpickr.js.org/" target="_blank">FlatPickr.</a>
Please see their docs for a complete set of instructions.

<Preview>
    <Story name="Demo">
        {{
            components: { 
                FDatePicker 
            },
            data: () => {
                return {
                    config: { dateFormat: "Y-m-d" }
                }
            },
            template: '<f-date-picker :config="config" />'
        }}
    </Story>
</Preview>
